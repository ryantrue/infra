apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ai-ryandev-ru
  namespace: openwebui
spec:
  secretName: ai-ryandev-ru-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ai.ryandev.ru
