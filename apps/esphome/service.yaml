apiVersion: v1
kind: Service
metadata:
  name: esphome
  namespace: esphome
spec:
  type: ClusterIP
  selector:
    app: esphome
  ports:
    - name: http
      port: 6052
      targetPort: 6052
      protocol: TCP
