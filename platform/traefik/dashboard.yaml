apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: kube-system
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`traefik.ryandev.ru`)
      kind: Rule
      middlewares:
        - name: basic-auth
      services:
        - name: api@internal
          kind: TraefikService
  tls:
    secretName: ryandev-ru-wildcard-tls
