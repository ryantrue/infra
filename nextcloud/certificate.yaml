apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloud-ryandev-ru
  namespace: nextcloud
spec:
  secretName: cloud-ryandev-ru-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - cloud.ryandev.ru
